/* @flow */

import React from 'react';
import { style } from 'glamor';

const bodyStyle = style({
  padding: '2rem',
})

const header = style({
  fontSize: '2rem'
})

export default () => (
  <div className={bodyStyle}>
    <h3 className={header}>Your next steps</h3>
    <p>Congratulations on starting your new knit project! You can get started by editing the files found under <code>modules/node_modules</code>.</p>
    <p>To see how these components depend on each other run <code>yarn knit -- list -d</code> in your terminal</p>
    <img width='800' src={require('./list.png')}/>
    <p>A dependencies list is built by scanning each module for required packages. This list can then be used to determine if you have missing or unused dependencies.</p>
    <p>Lets add a new dependency to <code>@myscope/page-component</code> without adding it to your root package.json</p>
<pre>
// @myscope/page-component<br/>
import 'lodash'
</pre>
    <p>Now when we run <code>yarn knit -- list -d</code> it will show the missing dependency:</p>
    <img width='800' src={require('./list_missing.png')}/>
    <p>You can also check for missing dependencies by running <code>yarn knit -- validate</code>.</p>
    <p>Learn more about <code>validate</code> here.</p>
    <img width='800' src={require('./validate_missing.png')}/>
    <p>We can fix this by installing lodash with Yarn:</p>
<pre>
yarn add lodash
</pre>
    <img width='800' src={require('./valid.png')}/>
    <p>Now that we have things working lets break them again. Go back into <code>@myscope/page-component</code> and remove <code>import 'lodash'</code> and then re-run <code>validate</code>:</p>
    <img width='800' src={require('./validate_unused.png')}/>
    <p>Knit is comparing the comparing the dependencies found in your modules with the dependencies saved in your root package.json and can warn you if you have missing or unused packages!</p>
    <h3 className={header}>Learn more about KnitJS</h3>
    <p>You can learn more about what you can do with knit by visiting our <a href="https://github.com/knitjs/knit">github repo</a> or by running <code>yarn knit -- --help</code></p>
    <img width='800' src={require('./help.png')}/>
  </div>
)
