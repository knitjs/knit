/* @flow weak */

const babel = require('babylon');

const babelParser = content => (
  babel.parse(content, {
    sourceType: 'module',
    plugins: ['jsx', 'flow'],
  })
);

module.exports = babelParser;
