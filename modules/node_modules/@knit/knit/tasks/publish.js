/* @flow weak */

const execa = require('execa');
const path = require('path');
const Listr = require('listr');

const needle = require('@knit/needle');

const knit = require('@knit/knit');

const createPublishTask = (m) => ({
  title: m,
  task: ctx => execa('yarn', [
    '--version'//, path.join(needle.paths.modules, m, needle.paths.distStub)
  ])
});

const tasks = [
  {
    title: "publishing updated modules",
    task: ctx => new Listr(ctx.updated.map(createPublishTask), {concurrent: true})
  },
]

module.exports = tasks;
