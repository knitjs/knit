/* @flow */

const execa = require('execa');

const needle = require('@knit/needle');

const tasks = [
  {
    title: 'clean up after publish',
    task: () => execa('git', ['checkout', '--', `${needle.paths.modulesStub}/**/package.json`]),
  },
];

module.exports = tasks;
