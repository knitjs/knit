/* @flow weak */

const execa = require('execa');

const tasks = [
  {
    title: 'push new tag to git',
    task: ctx => execa('git', ['push', 'origin', ctx.branch, '--follow-tags']),
  },
];

module.exports = tasks;
