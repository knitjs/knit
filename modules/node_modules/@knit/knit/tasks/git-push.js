/* @flow */

const execa = require('execa');

type TCtx = {
  branch: string,
};

const tasks = [
  {
    title: 'push new tag to git',
    task: (ctx: TCtx) => execa('git', ['push', 'origin', ctx.branch, '--follow-tags']),
  },
];

module.exports = tasks;
