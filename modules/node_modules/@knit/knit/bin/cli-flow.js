/* @flow weak */

const execa = require('execa');
const flow = require('flow-bin');
const log = require('../lib/log');

module.exports = () => {
  execa.stdout(flow, ['check']).then(obj => {
    log.success(obj);
  }).catch(err => {
    console.log(err.stdout);
  });
};
