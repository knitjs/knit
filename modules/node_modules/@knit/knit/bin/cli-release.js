#!/usr/bin/env node

const Listr = require('listr');

const errors = require('../lib/errors');

module.exports = (version) => {
  new Listr([
    ...require('../tasks/preflight'),
    ...require('../tasks/version'),
  ]).run({ version }).catch(errors.catchErrors)
}
