/* @flow weak */

module.exports = (opts) => {
  if (opts.watch && !process.env.WATCH) {
    process.env.WATCH = opts.watch;
  }
  if (opts.ci && !process.env.CI) {
    process.env.CI = opts.ci;
  }

  require('../tasks/test');
};
