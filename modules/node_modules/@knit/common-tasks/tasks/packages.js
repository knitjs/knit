/* @flow weak */

const knit = require('@knit/knit-core');

const tasks = [
  {
    title: 'reading package.json of modules',
    task: ctx => {
      ctx.pkgs = {};
      ctx.modules.forEach(m => {
        ctx.pkgs[m] = knit.readPkg(m);
      });
    },
  },
];

module.exports = tasks;
