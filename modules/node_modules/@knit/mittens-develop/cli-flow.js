/* @flow weak */

const execa = require('execa');
const flow = require('flow-bin');
const errors = require('@knit/nice-errors');

module.exports = () => {
  execa(flow, ['check'], { stdio: 'inherit' }).catch(err => {
    if (err.stdout || err.stderr) errors.catchErrors(err);
  });
};
