/* @flow */

import type { TConfig } from '@knit/webpack-config-socks';

// $FlowIgnore
const webpackConfig: TConfig = require(`./${process.env.NODE_ENV || 'stage'}`); // eslint-disable-line import/no-dynamic-require

export default webpackConfig;
