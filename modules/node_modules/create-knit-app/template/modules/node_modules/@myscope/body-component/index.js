/* @flow */

import React from 'react';
import { style } from 'glamor';

const bodyStyle = style({
  padding: '2rem',
})

const header = style({
  fontSize: '2rem'
})

export default () => (
  <div className={bodyStyle}>
    <h3 className={header}>Your next steps</h3>
    <p>Congratulations on starting your new knit project! You can get started by editing the files found under <code>modules/node_modules</code>.</p>
    <p>To see how these components depend on each other run <code>yarn knit -- list -d</code> in your terminal</p>
    <img width='800' src={require('./list.png')}/>
    <h3 className={header}>Learn more about KnitJS</h3>
    <p>You can learn more about what you can do with knit by visiting our <a href="https://github.com/knitjs/knit">github repo</a> or by running <code>yarn knit -- --help</code></p>
    <img width='800' src={require('./help.png')}/>
  </div>
)
