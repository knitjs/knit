/* @flow */

import React from 'react';
import { style } from 'glamor';

const headerStyle = style({
  display: 'flex',
  justifyContent: 'center',
  alignContent: 'center',
});

const logoStyle = style({
  width: '17rem',
  marginRight: '2rem',
});

const titleStyle = style({
  fontSize: '10rem',
  margin: 0,
});

const subtitleStyle = style({
  fontSize: '2.7rem',
  margin: 0,
});

const titleContainer = style({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
});

export default () => (
  <header className={headerStyle}>
    <img className={logoStyle} src={require('./knit_logo.svg')}/>
    <div className={titleContainer}>
      <h1 className={titleStyle}>KnitJS</h1>
      <h2 className={subtitleStyle}>Untangle Your Javascript</h2>
    </div>
  </header>
)
