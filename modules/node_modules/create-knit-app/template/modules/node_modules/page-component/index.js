/* @flow */

import React from 'react';
import { style } from 'glamor';

import Header from '@myscope/header-component';
import Body from '@myscope/body-component';

const pageStyle = style({
  width: '900',
  margin: '0 auto',
})

export default () => (
  <div className={pageStyle}>
    <Header />
    <Body />
  </div>
)
